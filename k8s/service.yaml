apiVersion: v1
kind: Service
metadata:
  name: cicd-app-service
  namespace: cicd-demo

spec:
  type: NodePort

  selector:
    app: cicd-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
